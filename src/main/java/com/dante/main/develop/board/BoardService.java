package com.dante.main.develop.board;

import com.dante.main.domain.board.Board;

public interface BoardService {

		void saveBoard(Board board);
		
		//Board findByBoard_id(Long board_id);
}
